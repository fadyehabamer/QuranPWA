/* Intentionally empty.
   This file used to re-apply the raw accent colour on top of the tuned one
   theme-preload.js derives (undoing its contrast fixes) and to register the
   service worker a second time. Both are handled by the shared scripts. */
